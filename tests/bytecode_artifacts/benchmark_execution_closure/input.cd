fun makeAdder(offset) {
  fun add(value) {
    return value + offset;
  }
  return add;
}

let add = makeAdder(1);
let mut i = 0;
let mut total = 0;
while i < 50000 {
  total = add(total);
  i = i + 1;
}
print total;
