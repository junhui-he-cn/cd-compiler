import "./lib.cd" as lib;

fun render(value: lib.Outcome): string {
  match value {
    lib.Outcome.Good(text) => { return text; }
    lib.Outcome.Bad(numberValue) => { return str(numberValue); }
  }
}

print render(lib.make());
