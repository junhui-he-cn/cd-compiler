import "./lib.cd" as lib;

fun render(value: lib.Outcome): string {
  match value {
    lib.Outcome.Good(message: text) => { return text; }
    lib.Outcome.Bad(code: numberValue) => { return str(numberValue); }
  }
}

print render(lib.make());
