struct Profile { score: number? }
struct User { profile: Profile }

fun read(user: User): number {
  if (user.profile.score == nil) {
    return 0;
  }
  return user.profile.score + 1;
}

print read(User { profile: Profile { score: 4 } });
print read(User { profile: Profile { score: nil } });
