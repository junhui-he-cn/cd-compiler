fun takesNumber(value: number): number {
  return value;
}

fun outer() {
  let mut maybe: optional<number> = 1;
  fun clear() {
    maybe = nil;
  }
  if (maybe != nil) {
    clear();
    print(takesNumber(maybe));
  }
}

outer();
