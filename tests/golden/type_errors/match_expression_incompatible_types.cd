enum Result {
  Ok(number),
  Err(string),
}

fun score(result: Result): number {
  return match result {
    Result.Ok(value) => value,
    Result.Err(message) => message,
  };
}

print(score(Result.Ok(1)));
