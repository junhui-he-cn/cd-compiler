fun invalid(values: [optional<number>], index: number): number {
  if (values[index] == nil) {
    return 0;
  }
  let mut i = index;
  i = i + 1;
  return values[i] + 1;
}

print invalid([41, 42], 0);
