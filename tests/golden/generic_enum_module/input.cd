import "./lib.cd" as lib;

fun render(value: lib.Result<number, string>): string {
  match value {
    lib.Result.Ok(value) => { return "ok:" + str(value); }
    lib.Result.Err(message) => { return "err:" + message; }
  }
}

print(render(lib.make()));
