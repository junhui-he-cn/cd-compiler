struct Box { value: number }
let maybe: number? = 42;

impl Box {
  fun show() {
    print maybe;
  }
}

let box = Box { value: 1 };
if (maybe != nil) {
  box.show();
  if (maybe != nil) {
    print maybe + 1;
  }
}
