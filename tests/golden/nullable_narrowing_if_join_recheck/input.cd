fun ensure(value: number?, flag: bool): number {
  if (flag) {
    if (value == nil) {
      return 0;
    }
  } else {
    if (value == nil) {
      return 0;
    }
  }
  return value + 1;
}

print ensure(nil, true);
print ensure(41, true);
print ensure(nil, false);
print ensure(41, false);
