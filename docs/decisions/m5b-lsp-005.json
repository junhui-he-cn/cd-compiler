{
  "schema_version": 1,
  "decision_revision": "m5b-2026-07-27-r5",
  "inventory_revision": "m0d-2026-07-22-r1",
  "decision_id": "M5B-LSP-005",
  "domain": "language-server-rename",
  "status": "resolved",
  "question": "What rename operation should cross the first single-document LSP boundary?",
  "decision": "Add textDocument/rename using shared reference ranges and a production-lexer name check, with full-document semantic preflight before returning a URI-scoped WorkspaceEdit.",
  "query": {
    "result": "WorkspaceEdit or null",
    "position_encoding": "UTF-16 code units mapped to source-local bytes",
    "edits": "declaration-inclusive TextEdit[] sorted by source range",
    "name_validation": "one non-keyword identifier token",
    "preflight": "current document must be clean and renamed source must parse and type-check"
  },
  "shared_services": [
    "DeclarationIndex reference metadata",
    "DeclarationIndex source ranges",
    "Lexer",
    "FrontendSession",
    "TypeChecker"
  ],
  "compatibility_boundary": {
    "documents": "single document per URI",
    "imports": "existing stdin import rejection remains",
    "protocol_ids": "snapshot-local IDs are not serialized"
  },
  "quantitative_gate": "tests/lsp_tests.py covers function rename edits and invalid-name rejection.",
  "out_of_scope": [
    "cross-file and cross-module rename",
    "prepare-rename",
    "completion and signature help",
    "structured rename-conflict diagnostics",
    "incremental synchronization",
    "persistent semantic caches"
  ],
  "verification": {
    "focused_commands": [
      "cmake --build build --target compiler_design",
      "ctest --test-dir build --output-on-failure -R '^language_server$'",
      "python3 tests/lsp_tests.py ./build/compiler_design",
      "git diff --check"
    ]
  }
}
