{
  "schema_version": 1,
  "decision_revision": "m5b-2026-07-27-r16",
  "inventory_revision": "m0d-2026-07-22-r1",
  "decision_id": "M5B-LSP-016",
  "domain": "language-server-typed-struct-method-completion",
  "status": "resolved",
  "question": "How should completion expose methods for known struct receivers?",
  "decision": "Use member-call receiver TypeInfo and source declarations for the resolved local or opened struct, retaining TypeChecker signature detail.",
  "query": {
    "receiver": "member-call site with a known named struct TypeInfo",
    "types": "local, direct-import, namespace-alias, and export-from struct declarations",
    "items": "Method kind 2 with resolved signature detail",
    "ordering": "method name, source ID, source start, source end"
  },
  "shared_services": [
    "AnalysisSnapshot",
    "DeclarationIndex typed expressions and method declarations",
    "FrontendSession module graph",
    "exported type resolver",
    "TypeChecker resolved signatures"
  ],
  "compatibility_boundary": {
    "sources": "currently open virtual documents only",
    "enum_and_namespace_values": "qualified enum and namespace value paths retain existing completion behavior",
    "ids": "snapshot-local IDs are not serialized"
  },
  "quantitative_gate": "tests/lsp_tests.py covers the g prefix on re-exported api.Box.get.",
  "out_of_scope": [
    "unknown or dynamic receiver methods",
    "unopened dependencies",
    "enum constructors as methods",
    "namespace value members as methods",
    "persistent semantic caching",
    "range-based synchronization"
  ],
  "verification": {
    "focused_commands": [
      "cmake --build build -j2",
      "python3 tests/lsp_tests.py ./build/compiler_design",
      "ctest --test-dir build --output-on-failure -R '^language_server$'",
      "git diff --check"
    ]
  }
}
