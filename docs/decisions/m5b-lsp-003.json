{
  "schema_version": 1,
  "decision_revision": "m5b-2026-07-27-r3",
  "inventory_revision": "m0d-2026-07-22-r1",
  "decision_id": "M5B-LSP-003",
  "domain": "language-server-reference-queries",
  "status": "resolved",
  "question": "Which reference query should cross the first single-document LSP boundary?",
  "decision": "Add textDocument/references using shared DeclarationIndex lexical references, TypeChecker binding metadata, and source-local ranges without serializing snapshot-local IDs.",
  "query": {
    "result": "deterministic Location[]",
    "position_encoding": "UTF-16 code units mapped to source-local bytes",
    "include_declaration": "optional boolean, defaults to false",
    "type_error_documents": "retain parseable declaration/reference index",
    "incomplete_documents": "empty result"
  },
  "shared_services": [
    "DeclarationIndex",
    "TypeChecker binding metadata",
    "Program source ranges"
  ],
  "compatibility_boundary": {
    "documents": "single document per URI",
    "imports": "existing stdin import rejection remains",
    "protocol_ids": "snapshot-local IDs are not serialized"
  },
  "quantitative_gate": "tests/lsp_tests.py covers capability negotiation and references with includeDeclaration false and true.",
  "out_of_scope": [
    "rename and prepare-rename",
    "completion and hover/type information",
    "cross-module references",
    "persistent semantic caches",
    "range-based synchronization"
  ],
  "verification": {
    "focused_commands": [
      "cmake --build build --target compiler_design",
      "ctest --test-dir build --output-on-failure -R '^language_server$'",
      "python3 tests/lsp_tests.py ./build/compiler_design",
      "git diff --check"
    ]
  }
}
