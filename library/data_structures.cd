// Public compatibility entry point for the data-structure library.
//
// Implementations are grouped by dependency and topic. Existing clients can
// keep importing this file as ds; the facade preserves the original exports.

export Stack, Queue, Deque, RingBuffer, BinaryHeap, Option, Result, List,
       newStack, newQueue, newDeque, newRingBuffer, newBinaryHeap,
       some, none, ok, err, emptyList, prepend, head, tail, reverse, toArray,
       listLength, listGet, listAppend, listConcat, listTake, listDrop,
       mergeSortedLists, listMiddle, listIsPalindrome, listRemoveFromEnd
       from "./collections.cd";

export Tree, emptyTree, treeNode, treeLeaf, treeSize, treeHeight, treeLeafCount,
       treePreorder, treeInorder, treePostorder, treeLevelOrder, treeIsBalanced,
       treeMaxWidth, treeRootToLeafPaths, treeRootToLeafSums, bstContains,
       bstInsert, bstDelete, bstMinimum, bstMaximum, bstPredecessor,
       bstSuccessor, AvlTree, newAvlTree, RedBlackTree, newRedBlackTree
       from "./trees.cd";

export Set, OrderedSet, OrderedMapEntry, OrderedMap, BiMapEntry, BiMap, MultiSetEntry,
       MultiSet, MultiMap, newSet, newOrderedSet, newOrderedMap, newBiMap, newMultiSet,
       newMultiMap
       from "./sets.cd";

export LruCacheEntry, LruCache, newLruCache, LfuCacheEntry, LfuCache, newLfuCache
       from "./caches.cd";

export DisjointSet, Graph, WeightedEdge, WeightedGraph, SignedWeightedEdge,
       SignedWeightedGraph, BellmanFordError, BellmanFordResult,
       newDisjointSet, newGraph, newWeightedGraph, newSignedWeightedGraph,
       breadthFirstOrder, depthFirstOrder, connectedComponents, isBipartite,
       articulationPoints, bridges, eulerTrail, shortestDistances, shortestPath,
       inDegrees, topologicalOrder, hasCycle, stronglyConnectedComponents,
       bellmanFord, shortestWeightedDistances, allPairsWeightedDistances,
       maxFlow, minCut, shortestWeightedPath, minimumSpanningForest
       from "./graphs.cd";

export Trie, newTrie, reverseArray, rotateArray, linearSearch, compareStrings,
       stringLess, findSubstring, prefixFunction, zFunction, kmpSearch,
       isPalindrome, characterFrequency, areAnagrams,
       longestUniqueSubstringLength, longestPalindromicSubstring,
       isBalancedBrackets
       from "./strings.cd";

export FenwickTree, SegmentTree, SparseTable, newFenwickTree, newSegmentTree, newSparseTable
       from "./range_trees.cd";

export peakIndex, isMountainArray, mountainPeakIndex, lowerBound, upperBound,
       binarySearch, countValue, isSorted, sortArray,
       sortArrayInPlace, shellSort, selectionSort, selectionSortInPlace,
       bubbleSort, bubbleSortInPlace, mergeSort, quickSort, quickSortInPlace,
       heapSort, heapSortInPlace, chunkArray, slidingWindows,
       minimumSpanningForestKruskal
       from "./sorting.cd";

export prefixSums, differenceArray, prefixMinimums, prefixMaximums,
       nextGreaterValues, largestHistogramArea, Interval, mergeIntervals,
       intersectIntervals, selectNonOverlappingIntervals, minimumIntervalRooms,
       canReachEnd, minimumJumps, huffmanMergeCost, mergeSortedNumbers,
       twoSumSorted, threeSumZero, uniqueValues, intersectionValues,
       unionValues, differenceValues, frequencyEntries, mostFrequent,
       windowSums, maxWindowSum, maxSubarraySum
       from "./array_algorithms.cd";

export climbStairs, maxNonAdjacentSum, minCoinCount, uniqueGridPaths,
       uniqueGridPathsWithObstacles, minGridPathSum, editDistance,
       longestIncreasingSubsequenceLength, longestCommonSubsequenceLength,
       matrixChainCost, matrixPower2x2, mergeStonesCost, knapsack01,
       completeKnapsack, boundedKnapsack
       from "./dynamic_programming.cd";

export subsets, combinations, permutations, generateParentheses, nQueens,
       mazePaths
       from "./backtracking.cd";

export PriorityQueue, newPriorityQueue, MedianHeap, newMedianHeap,
       ExtendedGcdResult, gcd, lcm,
       extendedGcd, isPrime, sievePrimes, fastPower, factorial, fibonacci,
       primeFactors, divisors, binomialCoefficient, permutationCount,
       pascalTriangle, gcdArray, prefixProducts, minimumLargestPartitionSum
       from "./numeric.cd";
